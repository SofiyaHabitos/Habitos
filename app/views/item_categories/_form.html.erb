<%= form_for(item_category) do |f| %>
  <% if item_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_category.errors.count, "error") %> prohibited this item_category from being saved:</h2>

      <ul>
      <% item_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



<article class="content item-editor-page">
  <div class="title-block">
    <h3 class="title"> Add New Meal Course <span class="sparkline bar" data-type="bar"></span> </h3>
  </div>
  <form name="item">
    <div class="card card-block">
      <div class="row">
        <div class="form-group">
          <div class="col-xs-4">
            <label class="control-label">
              <%= f.label :category_id ,"Category Name" %>
            </label>
            <div class="form-group"> 
              <%= f.collection_select :category_id, Category.all.order('category_name'), :id, :category_name,{:prompt=>true} ,{class: "dropdown form-control boxed"}  %>
            </div>
          </div>
          
          <!-- <div class="col-xs-4">
            <label class="control-label">
              <1%= f.label :no_of_category,"No Of Items" %>
            </label>
              <1%= f.text_field :no_of_category, class: "dropdown form-control boxed"%>
          </div> -->
          
          <div class="col-xs-4">
            <label class="control-label">
              <%= f.label :super_category_name %>                  
            </label>
            <%= f.text_field :super_category_name, :class=>"form-control boxed"%>
          </div>
        </div>
      </div>
        
      <div class="form-group row mrgn-top">
            <div class="col-sm-10 col-sm-offset-2"> <%= f.submit :class=>"btn btn-primary"%></div>
      </div>
    </div>
  </form>
</article>
<% end %>


