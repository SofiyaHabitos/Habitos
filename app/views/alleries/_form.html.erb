<%= form_for(allery,validate: true) do |f| %>
  <!--% if allery.errors.any? %>
    <div id="error_explanation">
      <h2><!%= pluralize(allery.errors.count, "error") %> prohibited this allery from being saved:</h2>

      <ul>
      <!% allery.errors.full_messages.each do |message| %>
        <li><!%= message %></li>
      <!% end %>
      </ul>
    </div>
  <!% end %-->
  <article class="content item-editor-page wrap-center">
    <div class="title-block">
      <h3 class="title"><span class="sparkline bar" data-type="bar"></span> </h3>
    </div>
    <form name="item">
      <div class="row new_row">
        <div class="form-group">
          <div class="col-xs-4">
            <label class="control-label">
              <%= f.label :name ,"Name"%>
            </label>
            <div class="form-group"> 
              <%= f.text_field :name,required:true,class: "form-control boxed " %>
            </div>
          </div>
        </div>
      </div>

      <div class="row new_row">
        <div class="form-group">
          <div class="col-xs-4">
            <label class="control-label">
              <%= f.label :description %>
            </label>
            <div class="form-group"> 
              <%= f.text_area :description,class: "form-control boxed " %>
            </div>
          </div>
        </div>
      </div>
        
      <div class="row new_row">
        <div class="col col-md-3"><%= link_to "Back", alleries_path ,:class=>"btn btn-primary"%></div>
        <div class="col col-md-3"><%= f.submit "Submit",:class=>"btn btn-primary"%></div>
      </div>
        
 <!-- <div class="form-group row mrgn-top">
        <div class="col-sm-10 col-sm-offset-4">
          <%#= f.submit "submit",:class=>"btn btn-primary"%>
        </div>
      </div> -->
    </form>
  </article>
<% end %>